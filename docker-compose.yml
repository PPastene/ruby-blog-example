version: '3.5'
services:
    
    web:
        build: .
        container_name: rails_dev
        command: bundle exec rails s -p 3000 -b '0.0.0.0'
        ports:
            - "3000:3000"
        volumes:
            - .:/rails
            - /node_modules
